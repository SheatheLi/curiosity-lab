# Bloom filter

A Bloom filter trades certainty for memory efficiency.

It can say:

- **definitely absent**
- **probably present**

It can produce false positives, but not false negatives for inserted items.

```bash
python labs/bloom_filter/bloom_filter.py
```
